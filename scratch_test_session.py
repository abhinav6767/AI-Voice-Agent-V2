import asyncio
from livekit.agents import AgentSession, Agent, TurnHandlingOptions

async def test():
    # just create mock objects if possible, or print types
    # Actually, we can just print the type of session.history
    print("Test script")

if __name__ == "__main__":
    asyncio.run(test())
